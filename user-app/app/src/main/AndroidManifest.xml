<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RiderApp">
        <activity
            android:name=".ui.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.RegisterActivity" />
        <activity android:name=".ui.VerifyOtpActivity" />
        <activity android:name=".ui.HomeActivity" />
        
        <!-- Admin Activities -->
        <activity 
            android:name=".ui.admin.SuperAdminDashboardActivity"
            android:exported="false" />
        <activity 
            android:name=".ui.admin.ListAdminActivity"
            android:exported="false" />
        <activity 
            android:name=".ui.admin.permissions.AdminPermissionsActivity"
            android:exported="false" />
        <activity 
            android:name=".ui.admin.CreateAdminActivity"
            android:exported="false" />
    </application>
</manifest>
